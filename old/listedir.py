def listedir(ch):
	""" 
	parameters : ch represents the path/destination;
	returns : the name of files in ch, if ch is a folder;
		      a liste containing the name of the file if ch is a file.
	"""
	